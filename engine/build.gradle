plugins {
    id 'java-library'
}

dependencies {
    api "com.google.dagger:dagger:$daggerVersion"
    api 'javax.inject:javax.inject:1'
}

sourceSets {
    main {
        resources {
            srcDir 'config'
            exclude '**/*.java'
        }
    }
}
